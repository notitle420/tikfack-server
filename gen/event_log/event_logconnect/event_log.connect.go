// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: event_log/event_log.proto

package event_logconnect

import (
	context "context"
	errors "errors"
	connect_go "github.com/bufbuild/connect-go"
	event_log "github.com/tikfack/server/gen/event_log"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect_go.IsAtLeastVersion0_1_0

const (
	// EventLogServiceName is the fully-qualified name of the EventLogService service.
	EventLogServiceName = "eventlog.EventLogService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EventLogServiceRecordProcedure is the fully-qualified name of the EventLogService's Record RPC.
	EventLogServiceRecordProcedure = "/eventlog.EventLogService/Record"
	// EventLogServiceRecordBatchProcedure is the fully-qualified name of the EventLogService's
	// RecordBatch RPC.
	EventLogServiceRecordBatchProcedure = "/eventlog.EventLogService/RecordBatch"
)

// EventLogServiceClient is a client for the eventlog.EventLogService service.
type EventLogServiceClient interface {
	// Record a single event.
	Record(context.Context, *connect_go.Request[event_log.RecordRequest]) (*connect_go.Response[event_log.RecordResponse], error)
	// Record a batch of events.
	RecordBatch(context.Context, *connect_go.Request[event_log.RecordBatchRequest]) (*connect_go.Response[event_log.RecordResponse], error)
}

// NewEventLogServiceClient constructs a client for the eventlog.EventLogService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEventLogServiceClient(httpClient connect_go.HTTPClient, baseURL string, opts ...connect_go.ClientOption) EventLogServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &eventLogServiceClient{
		record: connect_go.NewClient[event_log.RecordRequest, event_log.RecordResponse](
			httpClient,
			baseURL+EventLogServiceRecordProcedure,
			opts...,
		),
		recordBatch: connect_go.NewClient[event_log.RecordBatchRequest, event_log.RecordResponse](
			httpClient,
			baseURL+EventLogServiceRecordBatchProcedure,
			opts...,
		),
	}
}

// eventLogServiceClient implements EventLogServiceClient.
type eventLogServiceClient struct {
	record      *connect_go.Client[event_log.RecordRequest, event_log.RecordResponse]
	recordBatch *connect_go.Client[event_log.RecordBatchRequest, event_log.RecordResponse]
}

// Record calls eventlog.EventLogService.Record.
func (c *eventLogServiceClient) Record(ctx context.Context, req *connect_go.Request[event_log.RecordRequest]) (*connect_go.Response[event_log.RecordResponse], error) {
	return c.record.CallUnary(ctx, req)
}

// RecordBatch calls eventlog.EventLogService.RecordBatch.
func (c *eventLogServiceClient) RecordBatch(ctx context.Context, req *connect_go.Request[event_log.RecordBatchRequest]) (*connect_go.Response[event_log.RecordResponse], error) {
	return c.recordBatch.CallUnary(ctx, req)
}

// EventLogServiceHandler is an implementation of the eventlog.EventLogService service.
type EventLogServiceHandler interface {
	// Record a single event.
	Record(context.Context, *connect_go.Request[event_log.RecordRequest]) (*connect_go.Response[event_log.RecordResponse], error)
	// Record a batch of events.
	RecordBatch(context.Context, *connect_go.Request[event_log.RecordBatchRequest]) (*connect_go.Response[event_log.RecordResponse], error)
}

// NewEventLogServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEventLogServiceHandler(svc EventLogServiceHandler, opts ...connect_go.HandlerOption) (string, http.Handler) {
	eventLogServiceRecordHandler := connect_go.NewUnaryHandler(
		EventLogServiceRecordProcedure,
		svc.Record,
		opts...,
	)
	eventLogServiceRecordBatchHandler := connect_go.NewUnaryHandler(
		EventLogServiceRecordBatchProcedure,
		svc.RecordBatch,
		opts...,
	)
	return "/eventlog.EventLogService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EventLogServiceRecordProcedure:
			eventLogServiceRecordHandler.ServeHTTP(w, r)
		case EventLogServiceRecordBatchProcedure:
			eventLogServiceRecordBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEventLogServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEventLogServiceHandler struct{}

func (UnimplementedEventLogServiceHandler) Record(context.Context, *connect_go.Request[event_log.RecordRequest]) (*connect_go.Response[event_log.RecordResponse], error) {
	return nil, connect_go.NewError(connect_go.CodeUnimplemented, errors.New("eventlog.EventLogService.Record is not implemented"))
}

func (UnimplementedEventLogServiceHandler) RecordBatch(context.Context, *connect_go.Request[event_log.RecordBatchRequest]) (*connect_go.Response[event_log.RecordResponse], error) {
	return nil, connect_go.NewError(connect_go.CodeUnimplemented, errors.New("eventlog.EventLogService.RecordBatch is not implemented"))
}
